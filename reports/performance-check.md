# 性能检查报告

> 版本: v1
> 日期: 2026-06-26
> 任务: T1.3.2

## 检查项

### 构建产物

| 指标 | 结果 | 说明 |
|------|------|------|
| 构建成功 | 通过 | `logs/build.log` |
| 产物目录 | `dist/` | Vite 默认输出 |
| 主要产物 | `dist/assets/` | JS/CSS 资源 |
| 未检测到异常大文件 | 通过 | 静态检查 `dist/` 无异常 |

### 首屏性能

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 无阻塞渲染脚本 | 通过 | Vite 默认生成的模块脚本 |
| 字体异步加载 | 通过 | Google Fonts `@import` |
| 图片懒加载 | 通过 | `SiteCard` 使用 `loading="lazy"` |
| CSS 动画使用 transform/opacity | 通过 | 仅使用 transform/opacity |
| 无第三方追踪脚本 | 通过 | 未引入分析/广告脚本 |

### 运行时性能

- 推荐计算为纯函数，无 I/O
- 组件状态集中在 `App.tsx`，无深层 prop drilling
- 卡片列表最多渲染当前筛选结果，无虚拟滚动需求（31 条上限）

## 未执行项

- 未使用 Lighthouse 跑具体分数
- 未在真实弱网环境测试
- 未测量 FCP/LCP 具体数值

## 结论

T1.3.2 性能检查通过，未发现阻断性能问题。具体 Lighthouse 分数和弱网体验可在上线前补充。
